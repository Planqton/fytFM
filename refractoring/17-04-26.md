# FytFM Refactoring - Session 17.04.26

## Bearbeiteter Plan
`/home/planqton/.claude/plans/concurrent-finding-thacker.md`

---

## Heute erledigt

### 1. Quick Win Dialoge extrahiert
- `DeezerCacheDialogFragment.kt` (~110 Zeilen)
- `CorrectionsViewerDialogFragment.kt` (~115 Zeilen)
- Import-Fix: `RtCorrectionDao` → `at.planqton.fytfm.data.rdslog.RtCorrectionDao`

### 2. StateFlow Migration
- **Helper-Properties** für StateFlow in MainActivity:
  ```kotlin
  isRadioPlayingFromState
  isFmAmModeFromState
  isDabModeFromState
  currentFrequencyFromState
  currentDabServiceLabelFromState
  currentDabDlsFromState
  currentRdsPsFromState
  ```

- **Sync-Methoden** hinzugefügt:
  ```kotlin
  syncFmAmPowerState(isOn)
  syncDabPowerState(isOn)
  syncDabServiceInfo(serviceId, ensembleId, serviceLabel, ensembleLabel)
  syncDabDls(dls)
  ```

- **UI-Handler implementiert**:
  - `handleFmAmState()` - Frequenz, PS-Name, Power-Button
  - `handleDabState()` - Service-Label, DLS, Slideshow
  - `updatePowerButtonState()` - Button Alpha
  - `updateDeezerCoverFromStateFlow()` - Cover-Bilder
  - `updateDabSlideshowFromStateFlow()` - Slideshow-Bilder

### 3. Controller aktiviert
**FmAmController erweitert:**
- `setMute(mute: Boolean): Int`
- `getRssi(): Int`
- `openDev(): Boolean`
- `powerOnWithSteps(frequency: Float): Triple<Boolean, Boolean, Boolean>`

**DabController erweitert:**
- `getAudioSessionId(): Int`
- `isRecording(): Boolean`
- `startRecording(context, path): Boolean`
- `stopRecording()`
- `getCurrentEpgData(): EpgData?`
- Recording-Callbacks: `onRecordingStarted`, `onRecordingStopped`, `onRecordingError`
- `onAudioStarted`, `onEpgDataReceived`

**RadioController erweitert (Pass-through):**
- `setMute()`, `getRssi()`
- `setMonoMode()`, `setLocalMode()`, `setRadioArea()`
- `tuneDabService(serviceId, ensembleId)`
- `isDabRecording()`, `startDabRecording()`, `stopDabRecording()`
- `getDabAudioSessionId()`, `getDabEpgData()`
- `isDabOn()`, `hasDabTuner()`, `isDabAvailable()`
- `powerOnFmAmWithSteps()`, `powerOffFmAm()`, `powerOnDab()`, `powerOffDab()`
- Extended DAB-Callbacks: `onDabServiceStopped`, `onDabAudioStarted`, `onDabDlPlus`, etc.

**MainActivity migriert:**
- `fmNative.setMute()` → `radioController.setMute()`
- `dabTunerManager.tuneService()` → `radioController.tuneDabService()`
- `dabTunerManager.isDabAvailable()` → `radioController.isDabAvailable()`
- `dabTunerManager.getAudioSessionId()` → `radioController.getDabAudioSessionId()`
- `dabTunerManager.isRecording()` → `radioController.isDabRecording()`
- `dabTunerManager.startRecording()` → `radioController.startDabRecording()`
- `dabTunerManager.stopRecording()` → `radioController.stopDabRecording()`
- `dabTunerManager.getCurrentEpgData()` → `radioController.getDabEpgData()`
- `dabTunerManager.isDabOn` → `radioController.isDabOn()`
- `dabTunerManager.hasTuner()` → `radioController.hasDabTuner()`

---

## Aktueller Stand

| Metrik | Wert |
|--------|------|
| MainActivity Zeilen | 10.346 |
| Extrahierte Dialoge | 6 |
| Unit Tests | 83 (alle grün) |
| Direkte Hardware-Calls | 34 (von ~117) |

---

## Noch offen

### Hohe Priorität
1. **Verbleibende direkte Hardware-Calls (34)**
   - `toggleDabPower()` - Callback-Setups (~20 Calls)
   - `toggleRadioPower()` - TWUtil-Integration (~10 Calls)
   - `DabScanDialog` - braucht direkten `dabTunerManager` Zugriff

2. **TWUtil in Controller integrieren**
   - MCU-Steuerung (radioOn, unmute, setAudioSourceFm)
   - Aktuell direkt in MainActivity

### Mittlere Priorität
3. **PresetRepository reaktiv machen**
   - StateFlow für Stationslisten
   - Automatische UI-Updates bei Änderungen

4. **Weitere kleine Inline-Dialoge**
   - ~15 AlertDialogs noch inline
   - Jeweils 10-30 Zeilen

### Niedrige Priorität
5. **Private Variablen vollständig ersetzen**
   - ~150 private vars in MainActivity
   - Schrittweise durch StateFlow-Properties ersetzen

---

## Nächste Schritte (für morgen)

1. **Option A: TWUtil-Integration**
   - TWUtil-Aufrufe in RadioController/FmAmController verschieben
   - `toggleRadioPower()` vereinfachen

2. **Option B: DAB-Callbacks zentralisieren**
   - Callback-Setups aus `toggleDabPower()` nach DabController verschieben
   - MainActivity setzt nur noch RadioController-Callbacks

3. **Option C: PresetRepository reaktiv**
   - StateFlow für FM/AM/DAB Stationen
   - Stationsliste aktualisiert sich automatisch

---

## Wichtige Dateien

```
app/src/main/java/at/planqton/fytfm/
├── MainActivity.kt                 # 10.346 Zeilen
├── controller/
│   ├── RadioController.kt          # Hauptkoordinator (erweitert)
│   ├── FmAmController.kt           # FM/AM Logik (erweitert)
│   └── DabController.kt            # DAB+ Logik (erweitert)
├── viewmodel/
│   ├── RadioViewModel.kt           # 467 Zeilen
│   └── RadioUiState.kt             # Sealed class für UI State
├── ui/
│   ├── settings/SettingsDialogFragment.kt
│   ├── radioeditor/RadioEditorDialogFragment.kt
│   ├── logotemplate/LogoTemplateDialogFragment.kt
│   ├── bugreport/BugReportDialogFragment.kt
│   ├── cache/DeezerCacheDialogFragment.kt
│   └── corrections/CorrectionsViewerDialogFragment.kt
└── data/
    └── PresetRepository.kt         # Noch nicht reaktiv
```

---

## Build-Befehle

```bash
# Build & Install
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && ./gradlew installDebug

# Nur kompilieren
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && ./gradlew compileDebugKotlin

# Tests
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && ./gradlew test
```
