# FytFM Refactoring - Session 19.04.26

## Bearbeiteter Plan
`/home/planqton/.claude/plans/concurrent-finding-thacker.md`

---

## Heute erledigt

### 1. TWUtil-Integration in Controller

**FmAmController erweitert:**
- `powerOnFull(frequency: Float): Boolean` - Vollständige Power-On Sequenz inkl. TWUtil
- `powerOffFull()` - Vollständige Power-Off Sequenz inkl. TWUtil
- `applyTunerSettings()` - LOC, Mono, Area aus PresetRepository

**RadioController erweitert:**
- `powerOnFmAmFull(frequency: Float): Boolean` - Pass-through
- `powerOffFmAmFull()` - Pass-through
- TWUtil wird jetzt im Konstruktor übergeben

**MainActivity:**
- `toggleRadioPower()` vereinfacht: ~120 → ~45 Zeilen

### 2. DAB-Callbacks zentralisiert

**RadioController erweitert:**
- `onDabTunerReady: (() -> Unit)?` - Neuer Callback für Tuner-Ready

**MainActivity - Neue Handler-Methoden:**
- `setupDabCallbacks()` - Einmaliges Setup aller DAB-Callbacks bei Init
- `handleDabServiceStarted(dabStation)` - Service-Start Handler
- `handleDabDynamicLabel(dls)` - DLS Handler mit Deezer-Integration
- `handleDabSlideshow(bitmap)` - Slideshow Handler
- `handleDabAudioStarted(sessionId)` - Audio/Visualizer Handler
- `handleDabDlPlus(artist, title)` - DL+ Handler
- `handleDeezerResult(dls, trackInfo)` - Deezer-Result Handler

**toggleDabPower() vereinfacht:**
- Vorher: ~410 Zeilen (inline Callback-Setup bei jedem Power-Toggle)
- Nachher: ~35 Zeilen (nutzt RadioController, Callbacks bereits gesetzt)

### 3. Verbleibende Hardware-Calls migriert

**dabTunerManager → radioController:**
- `tuneService()` → `radioController.tuneDabService()` (2 Stellen)
- `stopService()/deinitialize()` → `radioController.powerOffDab()` (1 Stelle)
- **Ergebnis: 0 direkte dabTunerManager-Calls mehr!**

**fmNative → radioController:**
- `closeApp()`: `fmNative.powerOff()` → `radioController.powerOffFmAmFull()`
- Mode-Switch: `fmNative.powerOff()` → `radioController.powerOffFmAm()`
- Mode-Switch: `fmNative.powerOn()` → `radioController.powerOnFmAmFull()`
- **Verbleibend: 3 Calls in Seek-Funktion (RSSI-Messung - braucht direkten Zugriff)**

### 4. DlsLogDialog extrahiert

**Neue Datei:**
- `ui/dlslog/DlsLogDialogFragment.kt` - DialogFragment mit Callback-Interface

**MainActivity:**
- Implementiert `DlsLogDialogFragment.DlsLogCallback` Interface
- 5 neue Override-Methoden: `getDlsLogEntries()`, `getLastLoggedDls()`, `setLastLoggedDls()`, `saveDlsLogToFile()`, `getDlsLogFile()`
- `showDlsLogDialog()`: ~42 → 3 Zeilen

### 5. SettingsDialogFragment aktiviert

**MainActivity implementiert `SettingsDialogFragment.SettingsCallback`:**
- 33 Override-Methoden für Dialog-Callbacks
- `showSettingsDialogFragment()`: neue Funktion für DialogFragment
- 3 Aufrufe von `showSettingsDialog()` → `showSettingsDialogFragment()` migriert

**Legacy `showSettingsDialog()` gelöscht:**
- 728 Zeilen toter Code entfernt!
- Alle Settings jetzt via SettingsDialogFragment

---

## Aktueller Stand

| Metrik | Vorher | Nachher |
|--------|--------|---------|
| MainActivity Zeilen | 10.761 | 10.155 |
| dabTunerManager-Calls | 4 | 0 |
| fmNative-Calls | 7 | 3 (Seek) |
| Direkte Hardware-Calls | ~117 | ~3 |
| Inline Settings-Dialog | 728 | 0 |

**Fortschritt heute:**
- toggleRadioPower(): ~120 → ~45 Zeilen (-75)
- toggleDabPower(): ~410 → ~35 Zeilen (-375)
- showSettingsDialog(): ~728 → ~8 Zeilen (-720)
- DlsLogDialog: ~42 → ~3 Zeilen (-39)
- Hardware-Abstraktion: fast vollständig durch Controller
- **Gesamt: -606 Zeilen!**

---

### 6. DAB Handler-Methoden extrahiert (Fortsetzung)

**Neue Handler-Methoden in MainActivity:**
- `handleDabServiceStartedBase(dabStation)` - Gemeinsame Service-Start Logik
- `handleDabDynamicLabelBase(dls)` - Basis-DLS Handling ohne Deezer
- `handleDabDlPlus(artist, title)` - DL+ Handling
- `handleDabSlideshowBase(bitmap, sourceLabel)` - Slideshow Handling
- `handleDabDlsLogging(dls)` - DLS-Log Speicherung
- `handleDabDlsParserLog(dls)` - Parser-Logging
- `processDabDeezerSearch(dls)` - Komplette Deezer-Integration
- `handleDabDeezerResult(dls, trackInfo)` - Deezer-Ergebnis verarbeiten

**toggleMockDabPower() vereinfacht:**
- Vorher: ~165 Zeilen (inline Callbacks)
- Nachher: ~100 Zeilen (nutzt Handler-Methoden)

**toggleDabPower() stark vereinfacht:**
- Vorher: ~412 Zeilen (inline Callbacks)
- Nachher: ~265 Zeilen (nutzt Handler-Methoden)
- onDynamicLabel: ~150 → ~10 Zeilen
- onDlPlus: 14 → 4 Zeilen

---

## Aktueller Stand (Fortsetzung)

| Metrik | Nachher |
|--------|---------|
| MainActivity Zeilen | 10.173 |
| toggleDabPower() | ~265 Zeilen |
| toggleMockDabPower() | ~100 Zeilen |
| Gemeinsame DAB Handler | 8 Methoden |

**Code besser organisiert** - Handler-Methoden ermöglichen Wiederverwendung und Testbarkeit.

### 7. Weitere DialogFragments aktiviert

**CorrectionsViewerDialogFragment:**
- MainActivity implementiert `CorrectionsCallback` Interface
- `getRtCorrectionDao()` Override-Methode
- `showCorrectionsViewerDialog()` inline gelöscht (-55 Zeilen)

**DeezerCacheDialogFragment:**
- MainActivity implementiert `DeezerCacheCallback` Interface
- `getDeezerCache()` Override-Methode
- `showDeezerCacheDialog()` inline gelöscht (-56 Zeilen)

**BugReportDialogFragment:**
- MainActivity implementiert `BugReportCallback` Interface
- 4 Override-Methoden: `collectParserReportData`, `collectDeezerReportData`, `collectGeneralReportData`, `onBugReportGenerated`
- 5 inline Funktionen gelöscht (-153 Zeilen):
  - `showBugReportTypeDialog()`
  - `showParserBugReportDialog()`
  - `showDeezerBugReportDialog()`
  - `showBugReportInputDialog()`
  - `finalizeBugReport()`

---

## Aktueller Stand (Zwischenbilanz)

| Metrik | Wert |
|--------|------|
| MainActivity Zeilen | 9.655 → 9.089 |
| Aktivierte DialogFragments | 8 |
| AlertDialog.Builder | ~25 verbleibend |
| Duplizierter Code entfernt | ~247 Zeilen (Overlay Drag) |
| RadioEditorDialog migriert | -86 Zeilen |
| LogoTemplateDialog migriert | -566 Zeilen |

**🎉 Unter 9.100 Zeilen!**

### 8. Dialog-Analyse abgeschlossen

**21 verbleibende show*Dialog Funktionen analysiert:**

| Kategorie | Beispiele | Status |
|-----------|-----------|--------|
| Bereits optimiert (6 Zeilen) | `showEpgDialog`, `showDlsLogDialog` | ✓ Nutzt separate Klassen |
| Klein (< 20 Zeilen) | `showLanguageDialog`, `showNowPlayingAnimationDialog`, `showRdsRetentionDialog` | Kein Gewinn bei Extraktion |
| Mittel (30-80 Zeilen) | `showEditStationDialog`, `showEditDabStationDialog` | Unterschiedliche UIs |
| Groß (100+ Zeilen) | `showRadioEditorDialog`, `showLogoTemplateDialog` | Hoher Aufwand |

**Erkenntnis:** Die verbleibenden Dialoge sind entweder:
- Zu klein für sinnvolle Extraktion (< 20 Zeilen)
- Zu unterschiedlich für Konsolidierung
- Bereits in separaten Klassen (`EpgDialog`, `DlsLogDialogFragment`)

### 9. Debug Overlay Drag-Funktionen konsolidiert

**10 identische Funktionen → 1 generische Funktion:**

Vorher (je ~27 Zeilen × 10 = ~270 Zeilen):
```kotlin
private fun setupDebugOverlayDrag() { ... }
private fun setupDebugBuildOverlayDrag() { ... }
private fun setupDebugLayoutOverlayDrag() { ... }
// ... 7 weitere identische Funktionen
```

Nachher (~30 Zeilen):
```kotlin
private fun setupDebugOverlayDrag(overlay: View?, windowId: String) { ... }

// Aufrufe:
setupDebugOverlayDrag(debugOverlay, "rds")
setupDebugOverlayDrag(debugBuildOverlay, "build")
// ... etc.
```

**Ersparnis: -247 Zeilen!**

### 10. RadioEditorDialogFragment aktiviert

**Bereits existierendes DialogFragment gefunden und aktiviert:**
- `ui/editor/RadioEditorDialogFragment.kt` (483 Zeilen) - war ungenutzt!

**MainActivity implementiert `RadioEditorCallback` Interface:**
- `getRadioLogoRepository()`
- `getLogoForStation()`
- `getLogoForDabStation()`
- `onStationsUpdated()`
- `showManualLogoSearchDialog()` → `showManualLogoSearchDialogInternal()`
- `showEditStationDialog()` → `showEditStationDialogInternal()`
- `showEditDabStationDialog()` → `showEditDabStationDialogInternal()`
- `clearLastSyncedPs()`

**Inline `showRadioEditorDialog()` gelöscht:**
- 115 Zeilen inline Code entfernt
- ~38 Zeilen Callback-Methoden hinzugefügt
- **Netto: -86 Zeilen**

### 11. LogoTemplateDialogFragment aktiviert

**Bereits existierendes DialogFragment gefunden und aktiviert:**
- `ui/logotemplate/LogoTemplateDialogFragment.kt` (674 Zeilen) - war ungenutzt!

**MainActivity implementiert `LogoTemplateCallback` Interface:**
- `getCurrentRadioArea()`
- `onTemplateSelected()`
- `onLogosUpdated()`
- (getRadioAreaName und getRadioLogoRepository bereits in anderen Callbacks)

**Inline `showLogoTemplateDialog()` gelöscht:**
- ~85 Zeilen inline Code entfernt

**Helper-Funktionen gelöscht:**
- `updateEmptyState()`
- `selectLogoTemplate()`
- `exportLogoTemplate()`

**Tote nested Dialoge gelöscht:**
- `showTemplateEditorDialog()` (~110 Zeilen)
- `showStationEditorDialog()` (~150 Zeilen)
- `showImageSearchDialog()` (~90 Zeilen)
- `searchRadioLogos()` (~65 Zeilen)

**Ergebnis: 9.655 → 9.089 Zeilen (-566 Zeilen!)**

### 12. Kleine Optimierungen

**setRadioMuteState() Helper:**
- Duplizierte Mute-Logik in 3 Stellen konsolidiert
- Neue Helper-Funktion für alle Audio-Ausgänge
- **-28 Zeilen**

**Toter Code entfernt (Runde 1):**
- `saveStations()` - definiert aber nie aufgerufen
- `saveStationsForMode()` - definiert aber nie aufgerufen
- **-17 Zeilen**

### 13. Toter Code entfernt (Runde 2)

**Systematische Suche nach ungenutzten Funktionen:**

| Funktion | Zeilen |
|----------|--------|
| `updateDabListCover()` | -8 |
| `getNowPlayingAnimationName()` | -8 |
| `showLogoSearchDialog()` | -50 |
| `getAvailableMode()` | -22 |
| `showLogoSearchResultsDialog()` | -168 |
| `saveSingleLogo()` | -60 |
| `Int.dpToPx()` | -1 |

**Gesamt Runde 2: -329 Zeilen**

**Ergebnis: 9.044 → 8.715 Zeilen (-329 Zeilen)**

### 14. Architektur-Problem identifiziert

**Controller werden NICHT verwendet!**

MainActivity hat:
- 9 direkte `fmNative.*` Aufrufe
- Eigene `applyTunerSettings()` (Duplikat!)
- Eigene `startRdsPolling()` mit ~200 Zeilen
- 133 `findViewById` Aufrufe

FmAmController/RadioController existieren aber:
- Werden nie instanziiert in MainActivity
- Doppelte Logik (applyTunerSettings)
- RDS Callbacks werden nicht genutzt

**Nächster großer Schritt:** Controller tatsächlich integrieren.

### 15. Analyse abgeschlossen

**Verbleibende große Funktionen (nicht leicht zu refactoren):**
| Funktion | Zeilen | Grund |
|----------|--------|-------|
| `toggleDabPower()` | 264 | Komplexe Callback-Struktur |
| `toggleMockDabPower()` | ~120 | Ähnlich aber mit Mock-Tuner |
| `startRdsPolling()` | 198 | RDS-Parsing Logik |
| `initViews()` | 193 | View-Initialisierung |
| `setRadioMode()` | 176 | Mode-Switching Logik |

**Keine weiteren ungenutzten Funktionen gefunden.**

**Codebase-Übersicht:**
| Datei | Zeilen |
|-------|--------|
| MainActivity.kt | 8.715 |
| SteeringWheelKeyManager.kt | 1.013 |
| RadioScanner.kt | 973 |
| PresetRepository.kt | 892 |
| FytFMMediaService.kt | 742 |
| DabTunerManager.kt | 714 |
| SettingsDialogFragment.kt | 701 |

---

## Aktueller Stand (Session Ende)

| Metrik | Wert |
|--------|------|
| **MainActivity Zeilen** | **8.715** ✓ |
| Aktivierte DialogFragments | 8 |
| Ersparnis heute (Session) | ~2.046 Zeilen |

---

## Fortsetzung 20.04.26

### 16. RadioController in MainActivity integriert

**Problem identifiziert:**
- RadioController und FmAmController existieren aber werden NICHT verwendet
- MainActivity hat duplicate `applyTunerSettings()` (20 Zeilen)
- Direkte Hardware-Calls statt Controller

**Lösung:**

1. **RadioController Property hinzugefügt:**
```kotlin
private lateinit var radioController: at.planqton.fytfm.controller.RadioController
```

2. **Initialisierung in onCreate:**
```kotlin
radioController = at.planqton.fytfm.controller.RadioController(
    context = this,
    fmNative = fmNative,
    rdsManager = rdsManager,
    dabTunerManager = dabTunerManager,
    presetRepository = presetRepository,
    twUtil = twUtil
)
radioController.initialize()
```

3. **FmAmController.applyTunerSettings()** von private auf public geändert

4. **RadioController Wrapper hinzugefügt:**
```kotlin
fun applyTunerSettings() = fmAmController.applyTunerSettings()
```

5. **MainActivity.applyTunerSettings() ersetzt:**
   - Vorher: 20 Zeilen
   - Nachher: 1 Zeile Delegation

### 17. fmNative Calls durch Controller ersetzt

**Ersetzt:**
- `fmNative.getrssi()` (2x) → `radioController.getRssi()`
- `fmNative.setMute(false)` → `radioController.setMute(false)`

**Verbleibend (komplexer):**
- `fmNative.powerOn/Off()` in Power-Sequenzen (mit TWUtil, RDS, etc.)
- `fmNative.tune()` in Seek-Funktion

### 18. Verbleibende große Inline-Dialoge

| Funktion | Zeilen | Kandidat für Extraktion |
|----------|--------|------------------------|
| `showManualLogoSearchDialogInternal()` | ~321 | Ja, komplex |
| `showEpgDialog()` | ~232 | Ja, eigene Klasse existiert |
| `showAreaTemplateDialog()` | ~119 | Mittel |
| `showImportStationsDialog()` | ~118 | Mittel |

---

## Aktueller Stand (Fortsetzung 20.04)

| Metrik | Wert |
|--------|------|
| **MainActivity Zeilen** | **8.701** |
| fmNative-Calls | 10 (Power-Sequenzen) |
| dabTunerManager-Calls | 14 (Callbacks + Init) |
| radioController integriert | ✓ |

### 19. Weitere dabTunerManager → radioController Ersetzungen

**Ersetzt:**
- `dabTunerManager.isDabAvailable(this)` → `radioController.isDabAvailable()`
- `dabTunerManager.getCurrentService()` → `radioController.getCurrentDabService()`
- `dabTunerManager.getAudioSessionId()` → `radioController.getDabAudioSessionId()`
- `dabTunerManager.isDabOn` → `radioController.isDabOn()`
- `dabTunerManager.hasTuner()` → `radioController.hasDabTuner()`
- `dabTunerManager.isRecording()` → `radioController.isDabRecording()`
- `dabTunerManager.startRecording/stopRecording()` → `radioController.startDabRecording/stopDabRecording()`
- `dabTunerManager.getCurrentEpgData()` → `radioController.getDabEpgData()`
- `dabTunerManager.tuneService()` (5 Stellen) → `radioController.tuneDabService()`
- `dabTunerManager.stopService()/deinitialize()` (2 Stellen) → `radioController.powerOffDab()`

**Verbleibend (komplexer):**
- 11 Callback-Zuweisungen (`dabTunerManager.on*`) - nutzen jetzt Handler-Funktionen
- 1 `dabTunerManager.initialize()`
- 10 fmNative-Calls in Power-Sequenzen

### 20. Callback-Extraktion: handleDabServiceStartedFull

**Neuer Handler:**
- `handleDabServiceStartedFull(dabStation)` - 65 Zeilen
- Enthält: Deezer-Cleanup, Cover-Lock-Logik, MediaSession Update

**Inline Callback ersetzt:**
- `dabTunerManager.onServiceStarted` - 70 Zeilen inline → 1 Zeile Handler-Aufruf

**Aktueller Stand: 8.705 Zeilen**

| Hardware-Calls | Anzahl |
|----------------|--------|
| radioController.* | 22 (neu) |
| dabTunerManager.* | 14 (Callbacks) |
| fmNative.* | 10 (Power-Seq.) |
| mockDabTunerManager.* | 18 (DEV-Modus) |

**Code-Qualität verbessert:**
- Handler-Funktionen erleichtern Tests
- Controller-Abstraktion für Hardware-Zugriffe
- Callbacks nutzen extrahierte Handler

### 21. setupRadioControllerDabCallbacks() hinzugefügt

**Neue Infrastruktur:**
- `setupRadioControllerDabCallbacks()` - ~90 Zeilen
- Setzt alle DAB-Callbacks auf radioController
- Nutzt Handler-Funktionen für Logik

**Nächster Schritt (offen):**
- Inline Callbacks in toggleDabPower() entfernen
- Dann ~80-100 Zeilen Ersparnis

**Aktueller Stand: 8.799 Zeilen** (temporär erhöht wegen Infrastruktur)

---

## Noch offen

### Hohe Priorität
1. **PresetRepository reaktiv machen**
   - StateFlow für Stationslisten
   - Automatische UI-Updates bei Änderungen

### Mittlere Priorität
2. **toggleDabPower/toggleMockDabPower konsolidieren**
   - Gemeinsame Callback-Setup Logik extrahieren
   - Interface für Tuner-Manager abstrahieren
   - Potential: ~100-150 Zeilen Ersparnis

3. **RDS-Polling in Controller verschieben**
   - `startRdsPolling()` ist 198 Zeilen
   - Gehört in FmAmController

### Niedrige Priorität
4. **Private Variablen → StateFlow**
   - ~195 private vars in MainActivity
   - Schrittweise durch StateFlow-Properties ersetzen

---

## Nächste Schritte

1. **Option A: PresetRepository reaktiv**
   - StateFlow für FM/AM/DAB Stationen
   - Stationsliste aktualisiert sich automatisch

2. **Option B: Weitere Dialoge extrahieren**
   - Kleine AlertDialogs zu DialogFragments

3. **Option C: toggleMockDabPower() refactoren**
   - Gleiche Struktur wie toggleDabPower()

---

## Wichtige Dateien

```
app/src/main/java/at/planqton/fytfm/
├── MainActivity.kt                 # 8.715 Zeilen (< 8.8k!)
├── controller/
│   ├── RadioController.kt          # Hauptkoordinator (TWUtil + DAB Callbacks)
│   ├── FmAmController.kt           # FM/AM Logik (powerOnFull/powerOffFull)
│   └── DabController.kt            # DAB+ Logik
├── viewmodel/
│   ├── RadioViewModel.kt           # 467 Zeilen
│   └── RadioUiState.kt             # Sealed class für UI State
├── ui/
│   ├── settings/SettingsDialogFragment.kt    # ← AKTIV
│   ├── dlslog/DlsLogDialogFragment.kt        # ← AKTIV
│   ├── editor/RadioEditorDialogFragment.kt   # ← AKTIV
│   ├── logotemplate/LogoTemplateDialogFragment.kt  # ← JETZT AKTIV
│   ├── bugreport/BugReportDialogFragment.kt  # ← AKTIV
│   ├── cache/DeezerCacheDialogFragment.kt    # ← AKTIV
│   └── corrections/CorrectionsViewerDialogFragment.kt  # ← AKTIV
└── data/
    └── PresetRepository.kt         # Noch nicht reaktiv
```

---

## Build-Befehle

```bash
# Build & Install
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && ./gradlew installDebug

# Nur kompilieren
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && ./gradlew compileDebugKotlin

# Tests
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && ./gradlew test
```
# Session 20.04.2026 - Callback-Konsolidierung

## Fortschritt
- **Start**: 8,799 Zeilen
- **Ende**: 8,657 Zeilen  
- **Reduktion**: 142 Zeilen

## Änderungen

### Controller-Callbacks vervollständigt
- `DabController.setupCallbacks()`: onAudioStarted, onRecordingStarted/Stopped/Error, onEpgDataReceived hinzugefügt
- `RadioController.initialize()`: setupExtendedDabCallbacks() Aufruf hinzugefügt

### Handler-Funktionen erweitert
- `handleDabSlideshowBase()`: Deezer-Cover-Check und MediaSession-Update hinzugefügt

### toggleDabPower() vereinfacht
- ~150 Zeilen inline Callbacks entfernt
- Nutzt jetzt `radioController.powerOnDab()`
- Callbacks werden zentral in `setupRadioControllerDabCallbacks()` registriert

### Mock-DAB konsolidiert  
- `setupMockDabCallbacks()`: Neue Funktion für Mock-Tuner Callbacks
- `toggleMockDabPower()`: Inline Callbacks entfernt (~70 Zeilen)

## Architektur-Verbesserungen
- Callbacks werden jetzt einmal bei Init registriert statt bei jedem Power-Toggle
- Callback-Chain: dabTunerManager → DabController → RadioController → MainActivity
- Alle `dabTunerManager`-Aufrufe aus MainActivity entfernt

## Nächste Schritte
- `fmNative`-Aufrufe durch Controller ersetzen (9 verbleibend)
- `startRdsPolling()` refactoren (~196 Zeilen)
- Dialog-Funktionen in DialogFragments extrahieren



### Zusätzliche Extraktion
- `handleFrequencyChange()`: Frequency-Change-Handler (~45 Zeilen) aus `setupListeners()` extrahiert
- `setupListeners()` nun übersichtlicher (Listener ist 3 statt 45 Zeilen)

## Finaler Stand
- **Zeilen**: 8,658
- **Reduktion Session**: ~140 Zeilen netto (8,799 → 8,658)
- **Architektur**: Deutlich verbesserte Callback-Struktur



### ViewBinding Integration
- ViewBinding in build.gradle.kts aktiviert
- `ActivityMainBinding` Property hinzugefügt
- `setContentView(binding.root)` implementiert
- Infrastruktur bereit für schrittweise View-Migration

## Aktueller Stand
- **Zeilen**: 8,665
- **ViewBinding**: ✅ Aktiviert und verwendbar
- **Schrittweise Migration**: Views können nun nach und nach auf binding.xyz umgestellt werden

---

# Session 21.04.2026 - fmNative Migration

## Fortschritt
- **Start**: 8,665 Zeilen
- **Ende**: 8,427 Zeilen
- **Reduktion**: 238 Zeilen

## Änderungen

### fmNative → RadioController Migration vollständig

**Alle fmNative-Aufrufe aus MainActivity entfernt:**

| Stelle | Vorher | Nachher |
|--------|--------|---------|
| `closeApp()` | `fmNative.powerOff()` + `twUtil?.radioOff()` | `radioController.powerOffFmAmFull()` |
| `setRadioMode()` powerOff | `fmNative.powerOff()` + try/catch | `radioController.powerOffFmAm()` |
| `setRadioMode()` powerOn | `fmNative.powerOn()` + try/catch | `radioController.powerOnFmAmFull()` |
| `setRadioMuteState()` | `fmNative.setMute()` | `radioController.setMute()` |
| `toggleRadioPower()` OFF | 8 Zeilen inline | 3 Zeilen via Controller |
| `toggleRadioPower()` ON | ~85 Zeilen (komplette Sequenz) | ~15 Zeilen via Controller |
| Seek-Funktion | `fmNative.tune()` | `radioController.tuneRaw()` |

### Controller-Erweiterungen

**FmAmController:**
- `tuneRaw(frequency: Float): Boolean` - Tune ohne Frequenz-Speicherung für Seek

**RadioController:**
- `tuneRaw(frequency: Float): Boolean` - Wrapper für Seek-Operationen

### toggleRadioPower() stark vereinfacht
- **Vorher**: ~95 Zeilen (komplette Power-Sequenz inline)
- **Nachher**: ~25 Zeilen (Controller-Delegation)
- Controller übernimmt: TWUtil, FmService, FmNative-Calls, RDS-Enable

## Ergebnis

| Metrik | Wert |
|--------|------|
| **MainActivity Zeilen** | **8,427** |
| fmNative-Calls in MainActivity | 0 (nur 1 Kommentar) |
| Hardware via Controller | ✅ Vollständig |

## Nächste Schritte
1. ~~**startRdsPolling()** refactoren~~ ✅ Erledigt
2. **Schrittweise ViewBinding-Migration**
3. **PresetRepository reaktiv machen** (StateFlow)

---

## startRdsPolling() Refactoring

### Vorher
- **197 Zeilen** komplexer inline Callback-Code
- Schwer zu lesen und testen

### Nachher
- **startRdsPolling()**: 29 Zeilen (nur Callback-Routing)
- 5 neue Handler-Methoden:
  - `handleRdsParserLog(rt, ps)` - Parser-Logging
  - `handleRdsDeezerSearch(rt, ps, pi)` - Deezer-Integration
  - `handleRdsRawRtUpdate(rt, ps, pi)` - Raw-RT ohne Deezer
  - `handleRdsPsSync(ps)` - PS Auto-Sync zu Presets
  - `handleRdsDebugUpdate(...)` - Debug-UI Update

### Vorteile
- Bessere Lesbarkeit
- Handler-Methoden sind wiederverwendbar
- Einzelne Handler können isoliert getestet werden
- Klare Trennung der Verantwortlichkeiten

**Aktueller Stand: 8,420 Zeilen**

---

## ViewBinding Migration (begonnen)

### Fortschritt
- **Start**: 258 findViewById-Aufrufe
- **Aktuell**: 204 findViewById-Aufrufe
- **Ersetzt**: 54 Aufrufe

### Ersetzte Views
| Kategorie | Anzahl |
|-----------|--------|
| RDS Debug Labels | 9 |
| Deezer Debug Views | ~45 |

### Beispiele
```kotlin
// Vorher
findViewById<TextView>(R.id.debugDeezerArtist)?.text = trackInfo.artist

// Nachher
binding.debugDeezerArtist.text = trackInfo.artist
```

### Fortschritt (Runde 2)
- **Start**: 204 findViewById-Aufrufe
- **Aktuell**: 165 findViewById-Aufrufe
- **Ersetzt**: 39 weitere Aufrufe

### Ersetzte Views (Runde 2)
| Kategorie | Anzahl |
|-----------|--------|
| Debug Build Views | 9 |
| Layout Views (stationBar, viewModeToggle) | 3 |
| Debug Buttons | 8 |
| Main UI Buttons | 7 |
| Parser Tab Buttons | 2 |
| Spinner, rootLayout | 3 |
| Weitere | ~7 |

### Nicht ersetzbar (separate Layouts)
- Archive-Overlay Views (include)
- PiP-Layout Views (include)
- Dialog Views

### Gesamt
- **Start**: 258 findViewById
- **Aktuell**: 165 findViewById
- **Ersetzt**: 93 (36%)

