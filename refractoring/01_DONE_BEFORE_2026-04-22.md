# Stand der Aufräumarbeiten vor dem 22.04.2026

Kurz-Inventar dessen, was vor dieser Session bereits erledigt war. Quelle:
`REFACTORING_STATUS.md` + `19-04-26.md` + `17-04-26.md` + `todo.md`.

## Gesamt-Fortschritt MainActivity

| Metrik | Vorher | Aktuell | Änderung |
|--------|-------:|--------:|---------:|
| MainActivity Zeilen | 10 761 | 7 883 | −27 % |
| Funktionen | ~380 | 337 + setupDialogListeners | −11 % |
| findViewById | 258 | 45 | −83 % |
| AlertDialog.Builder | 15 | 7 | −53 % |
| Toast-Aufrufe konvertiert | 0 | 72 / 74 | 97 % |
| fmNative-Aufrufe | viele | 0 | −100 % |
| onCreate() Zeilen | 228 | 91 | −60 % |
| setRadioMode() Zeilen | 160 | 35 | −78 % |
| saveCurrentCoverAsStationLogo | 172 | 35 | −80 % |
| updateDeezerDebugInfo | 139 | 55 | −60 % |

## Strukturelle Umbauten

### Controller-Architektur
Neue Klassen unter `app/src/main/java/at/planqton/fytfm/controller/`:
- `RadioController` — Koordinator (TWUtil, DAB-Callbacks, FM/AM-Wrapping)
- `FmAmController` — komplette FM/AM-Logik (`powerOnFull`, `powerOffFull`, `applyTunerSettings`, `tuneRaw`)
- `DabController` — DAB+ Logik inkl. `setupCallbacks()` für Recording/EPG/Audio

→ Alle direkten Hardware-Calls (`fmNative.*`, `dabTunerManager.*`) aus MainActivity verschwunden.

### Debug-Extraktion
- Neue Klasse `at.planqton.fytfm.debug.DebugManager` (382 Zeilen, 17 Funktionen) — alle Debug-Overlay-Updates.

### DialogFragments (8 von 15 migriert)
Neuer Ordner `app/src/main/java/at/planqton/fytfm/ui/dialogs/`:
- `ConfirmationDialogFragment`
- `SingleChoiceDialogFragment`
- `CrashReportDialogFragment`
- `InputDialogFragment`
- `ImportStationsDialogFragment`
- `TextPreviewDialogFragment`

Zusätzlich reaktiviert bzw. erstmalig angebunden (waren als Code schon da, aber ungenutzt):
- `ui/settings/SettingsDialogFragment` (−728 Zeilen Inline-Code aus MainActivity)
- `ui/editor/RadioEditorDialogFragment` (−86 Zeilen netto)
- `ui/logotemplate/LogoTemplateDialogFragment` (−566 Zeilen netto)
- `ui/dlslog/DlsLogDialogFragment`
- `ui/bugreport/BugReportDialogFragment` (−153 Zeilen)
- `ui/cache/DeezerCacheDialogFragment` (−56 Zeilen)
- `ui/corrections/CorrectionsViewerDialogFragment` (−55 Zeilen)

`setupDialogListeners()` bündelt alle `FragmentResultListener`.

### ViewBinding
- ViewBinding in `build.gradle.kts` aktiviert.
- `ActivityMainBinding`-Property in MainActivity.
- 213 von 258 `findViewById` → `binding.*` migriert.
- Verbleibend: 45 findViewById (21 in Dialog-`onCreateView`, 11 im PiP-Include, 13 im Archive-Include).

### Große Funktionen aufgeteilt
| Funktion | vorher | nachher | Methode |
|----------|-------:|--------:|---------|
| `setRadioMode()` | 160 | 35 | cleanup/init/restore/update-Teile ausgelagert |
| `onCreate()` | 228 | 91 | `initRepositories/initRadioController/initExternalServices/setupDabModeOnStartup/updateInitialMediaSession` |
| `saveCurrentCoverAsStationLogo()` | 172 | 35 | `animateCoverSaveGesture/getSelectedCoverSource/saveCoverSourceToFile/updateLogoInRepository/refreshUiAfterLogoSave` |
| `updateDeezerDebugInfo()` | 139 | 55 | Reset-Logik extrahiert, Debug-Updates in DebugManager |
| `startRdsPolling()` | 197 | 29 | `handleRdsParserLog/DeezerSearch/RawRtUpdate/PsSync/DebugUpdate` |
| `toggleRadioPower()` | ~95 | ~25 | Controller-Delegation |
| `toggleDabPower()` | ~412 | ~35–265* | zentrale `setupRadioControllerDabCallbacks()`, Handler-Methoden |
| `toggleMockDabPower()` | ~165 | ~100 | Handler-Methoden wiederverwenden |

\* Historie: 412 → 265 (Handler-Methoden) → 35 (zentral registrierte Callbacks).

### Handler-Methoden (wiederverwendbare Einheiten)
- RDS: `handleRdsParserLog`, `handleRdsDeezerSearch`, `handleRdsRawRtUpdate`, `handleRdsPsSync`, `handleRdsDebugUpdate`
- DAB: `handleDabServiceStartedBase/Full`, `handleDabDynamicLabel(Base)`, `handleDabSlideshow(Base)`, `handleDabAudioStarted`, `handleDabDlPlus`, `handleDabDlsLogging`, `handleDabDlsParserLog`, `processDabDeezerSearch`, `handleDabDeezerResult`
- Drag-Overlay: 10 identische `setup*OverlayDrag`-Funktionen → 1 generische Fkt. `setupDebugOverlayDrag(overlay, windowId)` (−247 Zeilen)

### Code-Qualität
- `TAG`-Konstante in allen neuen/refaktorierten Dateien (`private const val TAG = "fytFM"` in MainActivity, eigene TAGs in Dialogen).
- Toast-Helper `toast(String)` / `toast(@StringRes)`; 72 / 74 migriert.
- `setRadioMuteState()` konsolidiert drei duplizierte Mute-Blöcke.

### StateFlow / ViewModel-Infrastruktur (teilweise, Stand 17.04.)
- `RadioViewModel.kt` (~467 Zeilen) und `RadioUiState.kt` (sealed) existieren.
- Helper-Properties in MainActivity, die StateFlow lesen:
  `isRadioPlayingFromState`, `isFmAmModeFromState`, `isDabModeFromState`,
  `currentFrequencyFromState`, `currentDabServiceLabelFromState`,
  `currentDabDlsFromState`, `currentRdsPsFromState`.
- Sync-Methoden schreiben aktuelle Hardware-Zustände in den StateFlow:
  `syncFmAmPowerState`, `syncDabPowerState`, `syncDabServiceInfo`, `syncDabDls`.
- UI-Handler reagieren auf State-Änderungen:
  `handleFmAmState`, `handleDabState`, `updatePowerButtonState`,
  `updateDeezerCoverFromStateFlow`, `updateDabSlideshowFromStateFlow`.
- Noch offen: `PresetRepository` reaktiv machen, private `var`s vollständig durch StateFlow ersetzen.

### Tote Code-Läufe (2 Runden)
Gelöscht u. a.: `saveStations`, `saveStationsForMode`, `updateDabListCover`, `getNowPlayingAnimationName`, `showLogoSearchDialog`, `getAvailableMode`, `showLogoSearchResultsDialog`, `saveSingleLogo`, `Int.dpToPx`, interne Logo-Editor-/Logo-Template-Dialoge. Summe: ~346 Zeilen.

## Nicht-MainActivity-Arbeiten
- Dark Mode (System / Hell / Dunkel) in Settings.
- Alle hard-codierten UI-Strings nach `strings.xml` (EN + DE).
- Unit-Tests für `RtCombiner`, `PresetRepository`.
- `DeezerClient`: Retry-Logik, `DeezerError`-sealed-class für Fehlerpfade.

## Offene Punkte (Stand 22.04.2026 morgens)

### Hohe Priorität (laut verbesserung.md)
1. **MVVM/ViewModel** — `RadioViewModel`, `SettingsViewModel`, `RadioUiState`.
2. **UI-State als `StateFlow`**.
3. **Hilt-DI** einführen.

### Mittlere Priorität
- Restliche 7 komplexe Dialoge zu DialogFragments migrieren:
  - Radio Area Dialog, Area Template Selection (mit Download), Download Progress Dialog, Logo Download Error Dialog, Manual Logo Search Dialog (Image Grid), 2× Logo Save Dialogs.
- DAB+ UI-Feinschliff: eigenes DAB+-Icon, DLS in NowPlaying anzeigen.
- PresetRepository reaktiv machen (StateFlow für Stationslisten).

### Niedrige Priorität
- Include-Layouts per ViewBinding binden (PiP, Archive). ← *wird als Nächstes gemacht*
- Accessibility (Content Descriptions, Screen Reader).
- Coroutine-Lifecycle: explizite Cancellation bei Activity-Destroy.
- Weitere 195 private `var`s schrittweise durch StateFlow ersetzen.

---

## Session-Timeline

### Session 17.04.2026 (Start-Umfang: MainActivity 10 346 Zeilen)
- 2 neue DialogFragments extrahiert: `DeezerCacheDialogFragment`, `CorrectionsViewerDialogFragment`.
- StateFlow-Infrastruktur eingeführt (siehe Abschnitt oben): Helper-Properties, Sync-Methoden, UI-Handler.
- Controller massiv erweitert:
  - `FmAmController`: `setMute`, `getRssi`, `openDev`, `powerOnWithSteps`
  - `DabController`: `getAudioSessionId`, Recording-API, EPG-API, `onAudioStarted`, `onEpgDataReceived`
  - `RadioController`: Pass-throughs für all das
- 10 Hardware-Calls in MainActivity auf `radioController.*` umgestellt; 34 von ~117 noch direkt.
- Unit-Tests: 83, alle grün.

### Session 19.04.2026 (Start 10 761 → Ende 8 715)
Große Session. Kern-Ergebnisse:
- TWUtil in `FmAmController.powerOnFull/powerOffFull` integriert.
- `toggleRadioPower`: 120 → 45 Zeilen.
- `toggleDabPower`: 412 → 265 Zeilen durch Handler-Methoden, dann später 35 durch zentrale Callbacks.
- DAB-Callback-Handler in MainActivity extrahiert (`handleDabServiceStartedBase/Full`, `handleDabDynamicLabelBase`, `handleDabSlideshowBase`, `handleDabDlsLogging`, `handleDabDlsParserLog`, `processDabDeezerSearch`, `handleDabDeezerResult`, `handleDabDlPlus`).
- `DlsLogDialogFragment` extrahiert (`showDlsLogDialog` 42 → 3 Zeilen).
- `SettingsDialogFragment` aktiviert (Legacy-Inline `showSettingsDialog` −728 Zeilen).
- `RadioEditorDialogFragment` aktiviert (−86 Zeilen netto).
- `LogoTemplateDialogFragment` aktiviert (−566 Zeilen inkl. gelöschter Sub-Dialoge).
- `BugReportDialogFragment` aktiviert (−153 Zeilen).
- 10 identische `setup*OverlayDrag` → 1 generische Funktion (−247 Zeilen).
- Toter Code Runde 2: `updateDabListCover`, `getNowPlayingAnimationName`, `showLogoSearchDialog`, `getAvailableMode`, `showLogoSearchResultsDialog`, `saveSingleLogo`, `Int.dpToPx` → −329 Zeilen.

### Session 20.04.2026 (Start 8 799 → Ende 8 657)
- DAB-Callbacks komplett zentralisiert in `setupRadioControllerDabCallbacks()` (einmalige Registrierung bei Init statt pro Power-Toggle).
- `setupMockDabCallbacks()` für Mock-Tuner.
- Inline-Callbacks aus `toggleDabPower` und `toggleMockDabPower` entfernt (~220 Zeilen).
- `handleFrequencyChange()` aus `setupListeners()` extrahiert.
- ViewBinding in `build.gradle.kts` aktiviert, `ActivityMainBinding`-Property hinzugefügt.

### Session 21.04.2026 (Start 8 665 → Ende 8 427 → später weiter)
- `fmNative`-Migration abgeschlossen: alle Aufrufe in MainActivity ersetzt (Seek über `radioController.tuneRaw`, Power über Controller-Sequenzen).
- `startRdsPolling`: 197 → 29 Zeilen, Handler ausgelagert.
- ViewBinding-Migration Runde 1+2: 258 → 165 findViewById (−93).
- DebugManager extrahiert (17 Funktionen, 382 Zeilen).
- `saveCurrentCoverAsStationLogo`: 172 → 35 Zeilen.
- `updateDeezerDebugInfo`: 139 → 55 Zeilen.
- TAG-Konstanten vereinheitlicht.
- Toast-Helper eingeführt.

### Session 22.04.2026 (vor dieser Claude-Session)
- Toast-Konvertierung abgeschlossen: 72 / 74.
- ViewBinding-Migration Runde 3: 30 weitere Views; Stand 165 → 45 findViewById.
- 6 neue DialogFragment-Klassen in `ui/dialogs/` (Confirmation, SingleChoice, CrashReport, Input, ImportStations, TextPreview).
- 8 Dialoge auf DialogFragments umgestellt, `setupDialogListeners()` bündelt alle FragmentResultListener.
- AlertDialog.Builder: 15 → 7.

---

## Roh-Quellen (für Details bei Bedarf)
- `17-04-26.md` — Session 17.04. (Dialog-Extraktion, StateFlow-Infra, Controller-Ausbau).
- `19-04-26.md` — Sessions 19. – 21.04. (Controller-Integration, große Dialog-Migrationen, fmNative-Migration, Toten-Code-Entfernung, ViewBinding-Start, startRdsPolling).
- `REFACTORING_STATUS.md` — aktuelle Status-Snapshot-Datei (wird nach größeren Sessions gepflegt).
